import {Mode} from '../common/js/config'
const state = {
  musicList: {},
  playList:[],
  sequenceList:[],
  isPlaying: true,
  fullScreen:false,
  currentIndex:-1,
  playMode:Mode.sequence
}

export default state
