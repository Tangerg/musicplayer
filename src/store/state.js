import {playMode} from '../common/js/config'
const state = {
  musicList: {},
  playList:[],
  sequenceList:[],
  isPlaying: true,
  fullScreen:false,
  currentIndex:-1,
  playMode:playMode.sequence
}

export default state
